
'use strict';

//generate subset 

let permutationsSubSet = (function getSubset(sequence, radious) {

    function generatSubset(sequence, range){
        let sequenceSorted = sequence.sort();

        let centerIndex = sequenceSorted.indexOf(number);
    }

} (permutations, 1))

//end of subset generation

console.log("Neigbours in range : " + subSetRange + " in sequence of permutations of " + number);
console.log(permutationsSubSet);